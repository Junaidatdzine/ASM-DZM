# Cloud Functions non-secret config (loaded automatically by Firebase).
# Copy to `.env` and fill in real values. Do NOT commit the real `.env`.

# Comma-separated list of admin email addresses granted elevated access.
ADMIN_EMAILS=admin@example.com
